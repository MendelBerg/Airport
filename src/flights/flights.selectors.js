export const flightsListSelector = state => state.flights.flightsList;
export const inputValueSelector = state => state.flights.value;
export const pathSelector = state => state.flights.path;
export const searchSelector = state => state.flights.search;
